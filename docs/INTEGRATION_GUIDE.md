# Enjaz One - دليل ربط الواجهة الأمامية بالخلفية (Integration Guide) 🚀

مرحباً بك في الدليل الرسمي لربط منصة "إنجاز ون". تم تصميم هذا التطبيق ليعمل بشكل مرن مع أي Backend (ASP.NET CORE, Node.js, Python, etc) من خلال معمارية فصل البيانات (Decoupled Architecture).

---

## 1. فلسفة البيانات (Data Philosophy) 🧠
يعتمد التطبيق على مبدأ الـ **Mappers**. نحن لا نستخدم البيانات القادمة من السيرفر مباشرة في الواجهات، بل نقوم بتحويلها:
- **DTO (Data Transfer Object)**: شكل البيانات كما تأتي من قاعدة بياناتك (مثلاً: `itemTitle`).
- **Domain Model**: شكل البيانات داخل كود React (مثلاً: `title`).

> **لماذا؟** لكي لا ينهار التطبيق إذا قررت تغيير أسماء الحقول في قاعدة بياناتك؛ ستقوم فقط بتعديل ملف الـ Mapper المناسب.

---

## 2. الإعداد الأولي (Environment Setup) ⚙️
في ملف `.env` بجذر المشروع، قم بضبط المتغيرات التالية:
```env
# تحديد مصدر البيانات: local (للعرض التجريبي) أو api (للربط الحقيقي)
VITE_DATA_SOURCE=api

# الرابط الأساسي للسيرفر (بدون سلاش في النهاية)
VITE_API_BASE_URL=https://api.enjaz-one.com/v1
```

---

## 3. المصادقة والأمن (Authentication) 🔐
يعتمد النظام معيار **JWT Bearer Token**:
1. يتم تخزين التوكن في الـ `localStorage` تحت مفتاح `enjaz_session_token`.
2. يقوم الـ `httpClient.ts` بتضمين التوكن تلقائياً في رأس كل طلب (Header):
   `Authorization: Bearer <YOUR_TOKEN>`

### ⚠️ قاعدة ذهبية (AI Security)
**لا تضع مفتاح Gemini API في الفرونت-إند مطلقاً.** يجب أن يكون لدى السيرفر الخاص بك Endpoint تستقبل الطلب، وتستخدم المفتاح السري هناك، ثم تعيد النتيجة للفرونت-إند.

---

## 4. إعدادات السيرفر (CORS) 🌐
يجب أن يسمح السيرفر (Back-end) للفرونت-إند بالوصول. الإعدادات المطلوبة:
- **Allowed Headers**: `Content-Type`, `Authorization`, `Accept`.
- **Allowed Methods**: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`.

---

## 5. أمثلة للروابط (API Endpoints Contract)

### أ. المهام والعمليات (`/work-items`)
| العملية | الرابط | الطريقة | الوصف |
| :--- | :--- | :--- | :--- |
| جلب الكل | `/work-items` | `GET` | يعيد مصفوفة من `WorkItemDTO` |
| تحديث حالة | `/work-items/{id}/status` | `PATCH` | يرسل `{"status": "Done"}` |

**مثال للـ JSON المتوقع (WorkItemDTO):**
```json
{
  "id": "WI-1001",
  "itemTitle": "صب خرسانة الدور الأول",
  "itemStatus": "InProgress",
  "projectId": "P-001",
  "dueDate": "2023-12-30"
}
```

---

## 6. معالجة الأخطاء (Error Handling) 🛠️
يتوقع التطبيق استجابات HTTP قياسية:
- `200/201`: نجاح العملية.
- `401`: التوكن منتهي أو خاطئ (سيتم توجيه المستخدم لصفحة الدخول).
- `403`: المستخدم لا يملك صلاحية (Permissions).
- `422/400`: خطأ في البيانات المرسلة (Validation Error).

---

## 7. القواميس (Enums) المدعومة
يرجى مطابقة هذه القيم نصياً في السيرفر:
- **الحالات (Status)**: `Open`, `InProgress`, `PendingApproval`, `Approved`, `Rejected`, `Done`.
- **الأولويات (Priority)**: `Low`, `Medium`, `High`, `Critical`.

---
*تم إعداد هذا الدليل لضمان انتقال سلس من مرحلة التطوير إلى الإنتاج.*