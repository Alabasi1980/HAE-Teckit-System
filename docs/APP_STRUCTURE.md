# Enjaz One - دليل هيكلية التطبيق (Application Structure Analysis)

يقدم هذا المستند تحليلاً شاملاً لمكونات وصفحات تطبيق **Enjaz One**، والغرض من كل جزء، وكيفية ترابطها لخدمة دورة العمل.

---

## 1. المعمارية العامة (Architecture Overview)
يعتمد التطبيق على **System-Based Architecture** (معمارية مبنية على الأنظمة)، حيث يتم فصل كل مجال عمل (Business Domain) في مجلد مستقل تحت `systems/`.

*   **App.tsx**: المايسترو الرئيسي. يدير التوجيه (Routing) بناءً على الحالة `currentView`، ويحمل الحالة العامة للتطبيق (Global State) عبر `useEnjazCore`.
*   **shared/**: يحتوي على المكونات والخدمات التي تستخدمها عدة أنظمة (مثل زر الإشعارات، وخدمات الاتصال بقاعدة البيانات).

---

## 2. خريطة الصفحات والأنظمة (Sitemap & Purpose)

### 📊 نظام القيادة (Dashboard System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **Dashboard** | `systems/dashboard` | **مركز القيادة:** عرض مؤشرات الأداء الرئيسية (KPIs)، التنبيهات الحرجة، وحالة العمليات المفتوحة. تساعد الإدارة على اتخاذ قرارات سريعة. |

### ⚙️ نظام العمليات (Operations System)
*القلب التشغيلي لإدارة المهام والبلاغات.*

| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **WorkItemList** | `systems/operations` | **سجل العمليات:** عرض وجرد وتصفية جميع المهام والبلاغات. يدعم العرض كقائمة (List) أو لوحة كانبان (Kanban). |
| **ApprovalsView** | `systems/operations` | **مركز الاعتمادات:** واجهة مخصصة للمدراء لعرض الطلبات المعلقة التي تتطلب موافقتهم فقط، مع إمكانية القبول/الرفض السريع. |
| **WorkItemDetail** | `systems/operations` | **تفاصيل العملية (Modal):** النافذة المنبثقة الشاملة التي تعرض تفاصيل المهمة، سلسلة الموافقات، التعليقات، والتحليل الذكي (AI Analysis). |
| **CreateWorkItemModal** | `systems/operations` | **إنشاء طلب:** نموذج موحد لإنشاء أي نوع من العمليات (حادث، طلب صيانة، موافقة) مع دعم الحقول الديناميكية. |

### 🏗️ نظام المشاريع (Projects System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **ProjectsListView** | `systems/projects` | **محفظة المشاريع:** عرض بطاقات لجميع المشاريع مع ملخص مالي وزمني وحالة المخاطر لكل مشروع. |
| **ProjectDetail** | `systems/projects` | **لوحة المشروع:** صفحة تفصيلية شاملة لمشروع واحد. تحتوي على تبويبات فرعية: نظرة عامة، العمليات المرتبطة، الوثائق، الأصول في الموقع، وفريق العمل. |

### 📱 العمليات الميدانية (Field Ops System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **FieldOps** | `systems/field-ops` | **واجهة الجوال:** مصممة خصيصاً للاستخدام الميداني. تتيح رفع بلاغات الحوادث، طلبات الصيانة، ونداءات الاستغاثة (SOS) مع تحديد الموقع الجغرافي ودعم العمل دون إنترنت (Offline). |

### 📦 نظام الأصول (Assets System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **AssetsView** | `systems/assets` | **سجل الأصول:** تتبع المعدات والآليات. يتيح معرفة "من يملك العهدة؟"، حالتها الفنية، وقيمتها. يرتبط بـ AI لتحليل الحالة الفنية. |

### 📚 نظام المعرفة (Knowledge System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **KnowledgeBase** | `systems/knowledge` | **الويكي المؤسسي:** مكتبة للإجراءات والسياسات (SOPs). تحتوي على **AiWikiAssistant** للإجابة على أسئلة الموظفين بناءً على المحتوى الداخلي. |

### 📂 نظام الوثائق (Documents System)
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **DocumentsView** | `systems/documents` | **الأرشيف المركزي:** مكتبة رقمية لجميع المخططات والعقود. تدعم التصنيف، البحث، والتلخيص الذكي للمحتوى. |

### 👤 الملف الشخصي والإعدادات
| الصفحة/المكون | المسار | الغاية (Purpose) |
| :--- | :--- | :--- |
| **ProfileView** | `systems/profile` | **ملف الموظف:** عرض أداء الموظف، المهام الموكلة إليه، والعهد (الأصول) التي بحوزته حالياً. |
| **SettingsView** | `systems/settings` | **إعدادات النظام:** التحكم في "محرك الأتمتة" (Automation Rules)، إعدادات الذكاء الاصطناعي، والسياسات العامة. |

---

## 3. البنية التقنية والخدمات (Technical Architecture)

### أ. الخدمات الأساسية (`shared/services`)
هذه الخدمات هي المحرك الخفي للتطبيق:

1.  **`workItemsRepo`**: إدارة العمليات (CRUD)، إضافة التعليقات، ومعالجة سلسلة الموافقات.
2.  **`automationService`**: **"العقل المدبر"**. يطبق القواعد تلقائياً عند إنشاء مهمة (مثلاً: تعيين بلاغ الحريق لمسؤول السلامة فوراً).
3.  **`geminiService`**: واجهة الذكاء الاصطناعي لتحليل النصوص وتقديم التوصيات.
4.  **`notificationsRepo`**: نظام الإشعارات الداخلي.
5.  **`fieldOpsRepo`**: يدير التخزين المحلي (Local Storage) للمسودات عند انقطاع الإنترنت.

### ب. تدفق البيانات (Data Flow)
1.  **الحدث:** يقوم المستخدم بإجراء (مثل: "إنشاء بلاغ حادث").
2.  **الأتمتة:** يمر البلاغ عبر `automationService` للتحقق من القواعد وتعديل البلاغ (تعيين أولوية قصوى تلقائياً).
3.  **التخزين:** يتم حفظ البيانات عبر `Repo` المناسب.
4.  **التحديث:** يتم تحديث الحالة العامة في `useEnjazCore`، مما يؤدي لتحديث واجهات العرض (Dashboard, Lists) فوراً.

### ج. الذكاء الاصطناعي (Gemini Integration)
التطبيق مجهز بـ AI في النقاط التالية:
*   **تحليل العمليات:** زر "Analyze" في تفاصيل المهمة يعطي تقييم للمخاطر.
*   **المساعد المعرفي:** شات بوت في قسم المعرفة للإجابة من الوثائق.
*   **إدخال صوتي:** (في `FieldOps`) لتحويل الكلام إلى نصوص بلاغات (Mocked currently).
*   **تحليل الأصول:** تقييم حالة المعدات بناءً على سجل الصيانة.
