# Enjaz One - دستور قوانين التطوير (Dev Rules) 📜

هذا الملف هو المرجع الأول لأي مطور ينضم للمشروع. الالتزام بهذه القواعد **إلزامي** لضمان عدم تحول المشروع إلى "كود معقد" (Spaghetti Code).

---

## 1. هيكلية الأنظمة (System-Based Architecture) 🏗️
ممنوع تكديس الملفات في مجلدات عامة. كل ميزة جديدة يجب أن تكون "نظاماً" مستقلاً داخل مجلد `systems/`:
- **المكونات (Components)**: توضع داخل `systems/[feature]/components/`.
- **الخدمات (Services)**: توضع داخل `systems/[feature]/services/`.
- **المدخل الرئيسي**: يجب وجود ملف `index.ts` في جذر كل نظام لتصدير ما يحتاجه العالم الخارجي فقط.

---

## 2. قاعدة "البيانات هي الملك" (Data Layer) 📊
- لا تقم أبداً باستدعاء `fetch` أو `axios` مباشرة داخل المكون (Component).
- استخدم دائماً `useData()` لجلب الـ Repositories.
- أي بيانات جديدة يجب أن تمر عبر مجلد `data/mappers/` لضمان فصل الـ API عن الـ UI.

---

## 3. تنظيم الأكواد (Clean Code) 🧹
- **قاعدة الـ 200 سطر**: أي مكون يتجاوز 200 سطر برمجياً يجب تقسيمه إلى مكونات أصغر (Atomic Design).
- **المنطق (Logic)**: يوضع المنطق المعقد في Custom Hooks داخل مجلد `shared/hooks/` أو داخل `services` النظام.
- **التنسيق**: نستخدم Tailwind CSS فقط. يمنع كتابة CSS داخلي (Inline Styles) إلا للضرورات القصوى (مثل الحسابات الديناميكية).

---

## 4. الأمن والصلاحيات (RBAC) 🔐
- قبل إضافة أي زر "حساس" (حذف، تعديل مالي)، يجب تغليفه بـ `<PermissionGate>`:
  ```tsx
  <PermissionGate permission={PERMISSIONS.PROJECT_CREATE}>
     <Button>إضافة مشروع</Button>
  </PermissionGate>
  ```
- لا تضع منطق التحقق من الصلاحيات يدوياً؛ اعتمد على `rbacService`.

---

## 5. الذكاء الاصطناعي (AI Integration) 🤖
- أي ميزة تعتمد على Gemini يجب أن تكون اختيارية (Optional) ولا تؤثر على عمل التطبيق الأساسي في حال تعطل الـ API.
- يجب دائماً عرض "حالة تحميل" (Skeleton or Loader) أثناء انتظار رد الذكاء الاصطناعي.

---

## 6. العمل الجماعي و Git 🌿
- **الرسائل (Commits)**: يجب أن تكون وصفية بالإنجليزية (e.g., `feat: add asset transfer logic`).
- **التوثيق**: إذا أضفت Endpoint جديد، يجب تحديث `docs/INTEGRATION_GUIDE.md` فوراً.
- **اللغة**: الكود والتعليقات التقنية بالإنجليزية، والواجهة (UI) تدعم العربية بشكل أساسي مع مراعاة الـ RTL.

---

## 7. تجربة المستخدم (UX) ✨
- أي عملية "حذف" يجب أن تطلب تأكيداً من المستخدم.
- يجب أن يكون التطبيق متجاوباً (Responsive) 100% (Mobile-First).
- سرعة الاستجابة: استخدم `useMemo` و `useCallback` لتحسين الأداء في القوائم الطويلة.

---
**خالف هذه القواعد فقط إذا كنت مستعداً لشرح السبب في الـ Code Review!**